### 基本回顾
> 使用并查集，应当满足的关系为：传递性、对称性
##### 操作
1. 合并两个集合
2. 查询某元素所在集合的根结点
##### 优化
1. 路径压缩
2. 按秩合并（使用较少）



### 扩展
##### 记录每个集合的大小
用sz数组维护，该属性仅绑定到根结点上，合并时更新根结点的sz值
##### 记录每个点到根节点的距离（带权并查集）
需要绑定到每个元素上

这个属性可以用于维护集合内的多层关系

普通并查集只能按照集合划分元素，无法维护集合内的层级关系

这个值在合并时更新一次即可，路径压缩不会影响该值的正确性