#include <iostream>
#include <cstring>
#include <algorithm>

using namespace std;

const int N = 25;

int n, m;
char g[N][N];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    while (cin >> m >> n, n || m) {

    }

    return 0;
}
