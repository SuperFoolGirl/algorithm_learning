双向BFS，就是把搜索空间从两端同时进行搜索，直到两端的搜索相遇；这样就能把搜索空间缩小一半，从而提高效率

想象搜索树：如果每次扩展时分支太多，即子结点很多，那么指数级的空间复杂度无法承受；而双向BFS可以将扩展次数减半，也就限制了搜索树的深度，子结点们的数量就不会过于爆炸

本质是BFS的优化

应用场景：主要为“最小步数模型”

1.必须已知起点和终点

2.最短路一般不用，因为数据少，意义不大；最小步数模型一般是指数级别，有必要

代码操作：

需要两个队列

每次扩展队列元素较少的一端，以保证尽可能节省空间；而不是每次两边都同时扩展

当同一状态X分别从两个队列出队时，说明相遇