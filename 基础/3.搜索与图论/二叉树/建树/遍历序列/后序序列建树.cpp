// 必须借助数组来缓存完全二叉树的后序遍历

#include <iostream>
#include <cstring>
#include <algorithm>

using namespace std;

typedef long long LL;

const int N = 1e5 + 10;

struct Node {
    int l;
    int r;
    int val;
} tr[N];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    return 0;
}
